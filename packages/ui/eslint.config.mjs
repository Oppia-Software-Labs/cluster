// @cluster/ui ESLint flat config — extends the shared Cluster config (@cluster/config).
// ESLint 9 requires a flat config; the ui lint script (`eslint src`) resolves this file.
import base from "@cluster/config/eslint";

export default base;
